[project]
name = "fastapi-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.135.1",
    "watchdog>=6.0.0",
]

[tool.uv.workspace]
members = [
    "dumper",
]
